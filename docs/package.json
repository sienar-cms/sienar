{
	"private": true,
	"scripts": {
		"build": "npx @11ty/eleventy --output=build --formats=md,hbs",
		"serve": "npx @11ty/eleventy --serve --output=build --formats=md,hbs"
	},
	"devDependencies": {
		"@11ty/eleventy": "^1.0.1",
		"@11ty/eleventy-plugin-syntaxhighlight": "^4.0.0",
		"markdown-it": "^12.3.2"
	}
}
