@layout DashboardLayout
@attribute [Route(DashboardUrls.Index)]
@inject IComponentProvider ComponentProvider

<DocTitle Title="Dashboard"/>

@if (ComponentProvider.Dashboard is null)
{
	<Dashboard Categories="@([SienarMenus.UserManagement])"/>
}
else
{
	<DynamicComponent Type="ComponentProvider.Dashboard" />
}